import styled from "styled-components";
import {
  layout,
  space,
  flexbox,
  border,
  typography,
  position,
  color,
  size,
} from "styled-system";

export const Box = styled("div")(
  layout,
  space,
  flexbox,
  border,
  typography,
  position,
  color,
  size
);
